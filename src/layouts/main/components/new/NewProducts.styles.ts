import styled from "@emotion/styled";

export const NewProducts = styled.div`
  display: flex;
  --gap-size: 15px;
  gap: var(--gap-size);
  .left-product {
    display: flex;
    align-items: center;
    gap: var(--gap-size);
  }
`;
